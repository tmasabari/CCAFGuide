# Testing Standards

- Prefer the project's existing test framework and conventions.
- Add or update tests with behavior-focused assertions.
- Run the smallest relevant test set first, then broader validation when practical.
- Never claim a test passed unless the command actually ran successfully.
- Do not call external services from unit tests unless the test is explicitly an integration test.
